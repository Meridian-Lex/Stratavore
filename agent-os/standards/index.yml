version: "1.0"
generated: 2026-03-08T12:00:00+00:00
project: Stratavore

api:
  http-handlers:
    description: HTTP handler shape, route structure, respondJSON helper, and error response patterns

database:
  conventions:
    description: Migration naming, TIMESTAMPTZ, UUID PKs, JSONB usage, and pgxpool configuration

go:
  architecture:
    description: Ports-and-adapters interfaces, options structs, error wrapping with %w, and zap logger injection

sprint:
  lifecycle:
    description: Sprint/task state machines, sequence_number ordering, model_registry pattern, and execution audit
